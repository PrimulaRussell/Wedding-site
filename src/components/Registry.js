import React from 'react';
import '../styles/registry.css';

const Registry = () => {
    return (
        <div>
            <h1 className='registry-title'> Registry</h1>
            <p className='gift-request'> We would like gift cards</p>
        </div>
    );
};

export default Registry;